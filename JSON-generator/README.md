# Micro-Apps

## JSON-generator

This is a simple JSON generator that allows users to create JSON objects with custom properties and values.

## Instructions

1. Open `index.html` in your browser
2. Add custom properties and values
3. Click "Generate JSON" to see the JSON object

## Notes

- The API key is not included in the code for security reasons
